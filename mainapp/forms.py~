from django import forms

class IdeaForm(forms.Form):
    idea_title = forms.CharField(required=True)
    idea_text = forms.CharField(required=True)
    
class CommentForm(forms.Form):
    comment_text = forms.CharField(required=True)
